const assert = require('assert');

const api = require('../news_api');

describe('Search', function() {
    describe('#Search', function() {
        it('keyword1', function() {
            api.search(keyword1)
        });
    });
});